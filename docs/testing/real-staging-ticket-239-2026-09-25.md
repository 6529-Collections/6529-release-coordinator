# Real staging ticket #239 — September 25, 2026

This is the first completed real-profile staging trial for the frontend-only,
no-database-change path. It is **not** production, backend-service, monitoring,
database-change, or failure/recovery acceptance.
The operator selected only [Issue #239](https://github.com/6529-Collections/6529-release-coordinator/issues/239), submitted by `simo6529` for frontend [PR #4093](https://github.com/6529-Collections/6529seize-frontend/pull/4093) at `c0768baa886d9195be8dbf3db87f935681e549ed`, target staging, declared database change `no`.

Filtered run `744ab6e1-4aaa-4299-9777-623901c4cf9b` verified the request and PR, passed the exact Git rehearsal and combined Git check, then created temporary frontend [PR #4098](https://github.com/6529-Collections/6529seize-frontend/pull/4098). All five configured required checks passed on its exact head `33fce096d1166f19d88072648e9b15ef9ba34a1e`, including DCO for the Coordinator-authored commit. The Coordinator closed the temporary PR and verified deletion of its owned branch. Combined checks passed.

Before authorizing a product release, runtime identity verification stopped the run. The Coordinator's pinned backend `.github/workflows/deploy.yml` blob differed from the current workflow on both `1a-staging` and `main`. Both branches returned `4738363b457f5af9a2497b2854d1e5f78c241222`; the previous staging and production pins were `9644738f3e06f2ddc26cd5b63f138f1f8fbbbc6a` and `14565b6dabd5772f21f89f868e1029ae5a95aace`. A readback of every other pinned product workflow/evidence file at both environment refs found no mismatch.

The backend workflow diff adds `newsletterLoop` to the shared workflow's service inputs from backend [PR #2116](https://github.com/6529-Collections/6529seize-backend/pull/2116) and removes retired membership deployment options from backend [PR #2100](https://github.com/6529-Collections/6529seize-backend/pull/2100) and [PR #2105](https://github.com/6529-Collections/6529seize-backend/pull/2105). The same workflow file is present on both environment branches. Its `Check production preconditions` step exits unsuccessfully if `newsletterLoop` is selected with `environment=staging`, before deployment. The Coordinator keeps separate staging and production pins, even though their current values match; a later change to either branch still fails that branch's identity check. A readback on September 25 reconfirmed `4738363b457f5af9a2497b2854d1e5f78c241222` on each branch. These observations justify reviewing a pin refresh; they do not themselves authorize ignoring the mismatch.

The first invocation exited with `release_authorized: false`. No product integration PR, staging merge, deployment, E2E, or rollback ran then. Issue #239 remained open at `status:received`; the stopped run retained the journal lock for manual recovery. The original process exited. A new Coordinator process loads the current runtime configuration, and a prepared release calls `identity()` again before beginning; the old pin values are not saved in the journal lock.

Coordinator [PR #240](https://github.com/6529-Collections/6529-release-coordinator/pull/240) refreshed the separate staging and production pins after both backend workflow refs were read back, added independent branch-drift and identity-recheck tests, passed local and GitHub checks, and merged to `main` at `b15d7fb87009d8b20349a20062eed9a88a4d5392`. Before resuming, the operator verified frontend PR #4093 still had the ticket's exact head, was up to date with `main`, and had green checks; the backend workflow still matched both updated pins; Issue #239 was still received; the journal lock retained only Issue #239 and actor `simo6529`; and no prior Coordinator process was running. The documented stop-and-wait interval had elapsed.

The same filtered run `744ab6e1-4aaa-4299-9777-623901c4cf9b` resumed on September 25. It reverified intake and the source PR, then merged frontend staging integration [PR #4100](https://github.com/6529-Collections/6529seize-frontend/pull/4100). Frontend `1a-staging` read back at the exact merge commit `3f3d6ae40b6b2896fb8b296c1ba78a1f17d00e69`. The exact-artifact [staging deployment workflow](https://github.com/6529-Collections/6529seize-frontend/actions/runs/36119868694) completed successfully, including its deploy job. Its linked [staging E2E workflow](https://github.com/6529-Collections/6529seize-frontend/actions/runs/36120799404) completed successfully against the matching deployed frontend commit and backend staging commit `c5a092c7f879e366f395ede8b0a266210ad72587`, as verified in the Coordinator's saved operation report.

The Coordinator marked [Issue #239](https://github.com/6529-Collections/6529-release-coordinator/issues/239) `status:completed` with `reason:release-completed`, closed it, released the journal lock, and exited successfully. A separate readback confirmed the closed ticket, empty lock, exact frontend staging ref, and both workflow conclusions. The CLI's final `release_executed: true` and ticket's completed result describe this resumed release; its top-level `release_authorized: false` is not a failure of the completed saved release. This Coordinator run did not change a product production branch or environment, and no rollback was exercised.
