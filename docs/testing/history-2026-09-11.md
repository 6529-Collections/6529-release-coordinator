# Live history acceptance, September 11, 2026

## Scope and source

The operator authorized PR #66 delivery followed by sandbox migration and an
exact-repeat test. [PR #66](https://github.com/6529-Collections/6529-release-coordinator/pull/66)
merged as `f68294c729be00dd967c364c039983678907390c`. The two acceptance runs
started from that clean `main` checkout on Node 22.16.0. The Coordinator ran
locally; sample PR checks and services ran in GitHub Actions.

Before submission, the sandbox's unlocked `inbox-run-v4` journal held 13 tickets,
three completed batches and four completed standalone service attempts. It had
no archive files. Only older ticket #1 was open.

Submitted one fresh request through the ordinary private sandbox submitter,
creating [ticket #14](https://github.com/6529-Collections/release-coordinator-test-inbox/issues/14)
in [intake run 34600218040](https://github.com/6529-Collections/release-coordinator-test-inbox/actions/runs/34600218040).
Request `4a41b469-3547-490f-8c63-8a70be38097e` declares staging, no database
change, and the existing case C sample PRs:

- Backend [PR #16](https://github.com/6529-Collections/release-coordinator-test-backend/pull/16): `540d7e93fbd85a20cd606580aab2049bc42d49eb`.
- Frontend [PR #14](https://github.com/6529-Collections/release-coordinator-test-frontend/pull/14): `c595a09edd9f9b67e60764a2881863bc67b36ea8`.

## Migration and fresh candidate: passed

Ran the ordinary command with no operator plan file:

```sh
RELEASE_COORDINATOR_PROFILE=sandbox npm run --silent inbox:run -- --json
```

Run `34d55b4e-08f9-4260-8dc5-7ef2f173cc2c` completed at 12:59:09 UTC after
15 minutes 34 seconds. Cheap filtering selected #14 and held #1. The batch
fingerprint was `4d8e42765952fae0f12c1a2478a42320c6f4ce80d6f020d2e963a5d6a0514d41`.
Its Git attempt `d7d84663-2745-442d-b636-c2142eece0c1` and check attempt
`14f59074-ed26-4b38-8ce7-8e37da98444b` both passed.

| Evidence | Verified result |
| --- | --- |
| Backend [trial PR #22](https://github.com/6529-Collections/release-coordinator-test-backend/pull/22), [CI](https://github.com/6529-Collections/release-coordinator-test-backend/actions/runs/34600925366) | Exact combined code passed required checks. |
| Frontend [trial PR #20](https://github.com/6529-Collections/release-coordinator-test-frontend/pull/20), [CI](https://github.com/6529-Collections/release-coordinator-test-frontend/actions/runs/34601076113) | Exact combined code passed required checks. |
| [Service run 34601250912](https://github.com/6529-Collections/release-coordinator-test-backend/actions/runs/34601250912) | Migrations loop, worker, API and frontend passed in order. Service attempt `c770e1d6-7e03-466d-ad49-b01991eebc8d` recorded temporary database cleanup. |
| Trial cleanup | Both temporary PRs closed unmerged; both owned branches returned 404 on readback. |
| Ticket #14 | Assigned verified submitter `simo6529`; managed comment `5634584719`, `batch:passed`, `reason:batch-selected` and `status:waiting`. A passing candidate does not claim a release. |
| Migration | All 13 original receipt/request identities and transition prefixes preserved. Closed tickets #2–13 and their GitHub presentations were unchanged. |
| Archives | Three older batches and four older standalone services moved unchanged into verified archives, alongside the new completed batch. All eight passed the production archive validator's identity, checksum, structure, completion and summary checks. |
| Active state | `inbox-run-v5`, zero active batches/services and no lock after completion. |

## Exact repeat: passed

Repeated the same unscoped command after the first process finished and released
its lock. Run `dc16efe1-2ddc-4f05-a916-a33374a4b1e4` completed at 13:07:40 UTC
after 4 minutes 45 seconds. It repeated current receipt/readiness/Git observations,
then loaded the same archived batch and reverified its original check attempt.

Pinned before/after snapshots proved:

- Identical selected ticket, batch fingerprint, archive content and file blob IDs.
- Identical original attempt IDs, results, deadlines and budgets; no fresh attempt.
- Zero new trial PRs or workflow runs in either sample repository.
- Identical ticket state, transitions, GitHub labels, assignments and comments;
  no duplicate decision or presentation write.
- All eight archives still verified, active records still empty and lock released.

Both commands returned **exit 2**, naming only older ticket #1 as remaining work.
That ticket retains `reason:rehearsal-unverified` and its Coordinator hold. The
new candidate passed; neither command claimed the whole inbox was clear or
authorized a release. Normal lock acquisition/release advanced the journal even
though the repeat needed no new ticket or test work.

## Test retirement: passed

After the repeat, `--issue 14 --close-test` retired only the new test ticket in
run `b4ec6641-a266-4947-8600-28ba19ef8607`, completing at 13:10:37 UTC with exit 0.
Readback confirmed `status:closed`, `reason:test`, the original request body,
receipt identity, managed comment ID and earlier transitions. All eight archives
and their index entries were unchanged. Every other ticket and both sample
repositories were unchanged; the journal was unlocked. Only older ticket #1
remained open. Retirement produced a complete separate run log.

## Evidence boundaries

Snapshots pinned the sandbox journal at:

| Point | Journal commit |
| --- | --- |
| Before submission/migration | `2d992c93bfeb8ec4ea35ee544e28e8f8137de75b` |
| First completed run | `720e56af2bc63f22a756e291dc6c77958bfbdb6e` |
| Exact repeat | `efdd598601d92a6ff23c4cf7559594c574e54cac` |
| Test retirement | `e192fbaf8e1f6c6347842f32d5f872f6500c33fc` |

Both sample main refs, all pre-existing PRs, backend runtime
`49d92ac76c9bf91520c82010afbae7f9e0fdbb39` and the real inbox journal stayed
unchanged. No product merge, package publication, real-journal migration or
deployment occurred.

Receipts, inputs, stdout/stderr, pinned snapshots and assertion results remain
under ignored `.release-coordinator/history-acceptance-20260911/`. Per-run logs
remain outside checkouts under `~/.6529-release-coordinator/logs/sandbox/1362580376/`.
The first log had mode `0600`, complete JSON output and verified events for all
four service steps. The unchanged repeat recorded evidence revalidation and no
trial-opening or service-dispatch events.

This run proves the ordinary live migration/reuse/cleanup path. More than 100
histories, corrupt/missing archives, lost API responses, competing writers and
interrupted resume remain separately recorded offline cases. No new takeover or
release-execution capability was added.
